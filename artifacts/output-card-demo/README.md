# Output card demo

This directory is a disposable visual fixture for checking how a turn renders
several file changes alongside a generated PDF.

- `report-source.html` is the editable source document.
- `generate_pdf.py` creates a small, dependency-free PDF.
- `output-card-demo.pdf` is the generated artifact.

