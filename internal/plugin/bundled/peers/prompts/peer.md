Use the Peers plugin to handle this peer-session request.

If no arguments are provided, list the available peer sessions clearly. If arguments identify a target and message, resolve the target to a stable session id and send one bounded request. If the arguments ask to accept or refuse incoming peer requests, update this session's peer policy. The target's terminal result will return automatically in a later turn.

Arguments:
{{args}}
