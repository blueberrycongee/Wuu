The user explicitly requests a persistent goal. Use the objective supplied with this command to call create_goal, then start working toward it. Set token_budget only if the user explicitly provides one. If no objective was supplied, ask for it. If an unfinished goal already exists, read it with get_goal and direct the user to Goal controls to resume or clear it. Do not replace an unfinished objective or infer a new one from earlier conversation.
