{
  "schemaVersion": 1,
  "id": "note-compaction",
  "name": "Context Notes (Experimental)",
  "description": "Experimental context compaction driven by hidden session-scoped Markdown notes.",
  "icon": { "path": "assets/icon.svg" },
  "version": "0.1.0",
  "defaultEnabled": false,
  "author": { "name": "Wuu" },
  "minimumWuuVersion": "0.1.0",
  "runtime": { "protocol": "wuu-plugin-v1", "command": "@wuu-official-helper:note-compaction", "timeout": 30 },
  "contributes": {
    "settings": {
      "checkpoint_interval_tokens": {
        "type": "number",
        "title": "Checkpoint interval (estimated tokens)",
        "description": "Fork a fresh Markdown continuation note after roughly this much new transcript. Experimental estimates are provider-neutral and intentionally approximate.",
        "default": 12000,
        "scope": "workspace",
        "apply": "live"
      }
    }
  },
  "interface": { "displayName": "Context Notes (Experimental)", "shortDescription": "Replace old context with session-scoped Markdown checkpoints.", "developerName": "Wuu", "category": "Developer Tools", "capabilities": ["Write"] },
  "officialNativeHelper": { "id": "note-compaction", "environment": "WUU_NOTE_COMPACTION_PLUGIN_HELPER", "executable": "wuu-note-compaction-plugin" }
}
